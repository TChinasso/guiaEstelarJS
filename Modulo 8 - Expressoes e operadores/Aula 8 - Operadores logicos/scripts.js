// Operadores logicos (logical operators)

// -2 valores booleanos, quando verificados, resultara em verdadeiro ou falso

let pao = false
let queijo = true

// AND && (atender ambas as condicoes)
console.log(pao && queijo)

// OR ||
console.log(pao || queijo)

// NOT ! (troca o valor booleano)
console.log(!pao)